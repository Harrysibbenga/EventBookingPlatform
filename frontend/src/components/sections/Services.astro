---
/**
 * Services section showcasing key event hire services.
 * Light theme to contrast the dark hero. Uses brand gold accents.
 */
 import { SERVICES } from '../../utils/constants'
import ServiceCard from '../ui/ServiceCard.astro'
---

<section id="services" class="relative py-24 bg-cream-50">
  <!-- soft gold wash -->
  <div aria-hidden="true" class="pointer-events-none absolute -top-24 right-1/4 h-72 w-72 rounded-full bg-brand-gold/10 blur-3xl"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16 animate-on-scroll">
      <span class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-white border border-brand-gold/30 shadow-sm mb-6">
        <svg class="w-7 h-7 text-brand-gold" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"/>
        </svg>
      </span>

      <h2 class="text-4xl font-bold text-neutral-900 mb-4">
        Our Event Hire Services
      </h2>

      <p class="text-lg sm:text-xl text-neutral-600 max-w-3xl mx-auto leading-relaxed">
        From stunning 4FT LED number displays to balloon decorations and complete party packages,
        we provide everything you need to make your celebration spectacular.
      </p>
    </div>

    <!-- Services Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8 mb-16">
      {SERVICES.map((service, index) => (
        <ServiceCard
          service={service}
          index={index}
          class="animate-on-scroll"
          style={`animation-delay: ${index * 100}ms`}
          cardClass="bg-white border border-neutral-200 rounded-2xl p-6 shadow-sm hover:shadow-gold-glow hover:-translate-y-0.5 transition-all duration-300"
          titleClass="text-xl font-semibold text-neutral-900"
          descClass="text-neutral-600"
          ctaPrimaryClass="inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 font-semibold text-brand-black bg-gold-gradient shadow-gold-strong transition-all duration-300 hover:shadow-gold-glow"
          ctaSecondaryClass="inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2.5 font-medium text-neutral-800 bg-white border border-brand-gold/30 transition-all duration-200 hover:bg-cream-50"
        />
      ))}
    </div>

    <!-- Call to Action -->
    <div class="text-center animate-on-scroll" style="animation-delay: 600ms;">
      <div class="rounded-2xl border border-brand-gold/20 bg-gradient-to-r from-white to-cream-50 p-8 lg:p-12">
        <h3 class="text-2xl lg:text-3xl font-bold text-neutral-900 mb-4">
          Ready to Book Your Event Hire?
        </h3>

        <p class="text-lg text-neutral-600 mb-8 max-w-2xl mx-auto">
          Let's discuss your celebration and create the perfect LED number display or balloon package for your special day.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <a
            href="/booking"
            class="inline-flex items-center justify-center gap-2 rounded-xl px-7 py-3 font-semibold
                   text-brand-black bg-gold-gradient shadow-gold-strong
                   transition-all duration-200 hover:shadow-gold-glow hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-brand-gold/30"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
            Book Consultation
          </a>

          <a
            href="/pricing"
            class="inline-flex items-center justify-center gap-2 rounded-xl px-7 py-3 font-semibold
                   text-neutral-800 bg-white border border-brand-gold/30
                   transition-all duration-200 hover:bg-cream-50 hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-brand-gold/20"
          >
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1" />
            </svg>
            View Pricing
          </a>
        </div>
      </div>
    </div>

    <!-- Process Steps -->
    <div class="mt-20 animate-on-scroll" style="animation-delay: 800ms;">
      <div class="text-center mb-12">
        <h3 class="text-3xl font-bold text-neutral-900 mb-4">
          Our Simple Hire Process
        </h3>
        <p class="text-lg text-neutral-600 max-w-2xl mx-auto">
          Easy booking process from enquiry to collection
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-4 gap-8 relative">
        <!-- step 1 -->
        <div class="relative text-center group">
          <div class="w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center
                      bg-white border border-brand-gold/30 group-hover:border-brand-gold/50 transition-colors">
            <span class="text-2xl font-bold text-brand-gold">1</span>
          </div>
          <h4 class="text-lg font-semibold text-neutral-900 mb-2">Contact & Enquiry</h4>
          <p class="text-neutral-600 text-sm">Get in touch with your date, event type, and requirements for an instant quote.</p>
          <span class="step-connector hidden md:block" />
        </div>

        <!-- step 2 -->
        <div class="relative text-center group">
          <div class="w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center
                      bg-white border border-brand-gold/30 group-hover:border-brand-gold/50 transition-colors">
            <span class="text-2xl font-bold text-brand-gold">2</span>
          </div>
          <h4 class="text-lg font-semibold text-neutral-900 mb-2">Booking Confirmation</h4>
          <p class="text-neutral-600 text-sm">Secure your date and choose your colours and styling options.</p>
          <span class="step-connector hidden md:block" />
        </div>

        <!-- step 3 -->
        <div class="relative text-center group">
          <div class="w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center
                      bg-white border border-brand-gold/30 group-hover:border-brand-gold/50 transition-colors">
            <span class="text-2xl font-bold text-brand-gold">3</span>
          </div>
          <h4 class="text-lg font-semibold text-neutral-900 mb-2">Delivery & Setup</h4>
          <p class="text-neutral-600 text-sm">Professional delivery and installation on your event day.</p>
          <span class="step-connector hidden md:block" />
        </div>

        <!-- step 4 -->
        <div class="relative text-center group">
          <div class="w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center
                      bg-white border border-brand-gold/30 group-hover:border-brand-gold/50 transition-colors">
            <span class="text-2xl font-bold text-brand-gold">4</span>
          </div>
          <h4 class="text-lg font-semibold text-neutral-900 mb-2">Collection</h4>
          <p class="text-neutral-600 text-sm">Enjoy your celebration while we collect everything afterwards.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
/* connector lines between steps on md+ */
.step-connector {
  position: absolute;
  top: 2rem;             /* aligns with circle center */
  right: -2rem;
  width: 4rem;
  height: 2px;
  background: linear-gradient(to right, rgba(212,175,55,0.25), rgba(212,175,55,0.15));
}

/* Service card entrance animations (kept) */
.animate-on-scroll {
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.6s ease-out;
}
.animate-on-scroll.animate-fade-in-up {
  opacity: 1;
  transform: translateY(0);
}
</style>

<script>
  // Intersection Observer for animations (kept)
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver(
      entries => entries.forEach(entry => {
        if (entry.isIntersecting) entry.target.classList.add('animate-fade-in-up')
      }),
      { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }
    )
    document.querySelectorAll('.animate-on-scroll').forEach(el => observer.observe(el))
  })
</script>
